# 2026-04-20 工作记录

## 费率表边界值提取任务

- 文件：`f:\workbuddy\test_Jhs\费率表.xlsx`（Sheet：标准体费率表）
- 产品：瑞泰鸿利传世（致享版）终身寿险（分红型）
- 表格结构：行4=责任计划(0-7)，行5=保险期间(全终身)，行6=交费期间(1/3/5/10/15/20/30年)，行7=性别(男/女)，行8-77=年龄(0-69岁)数据
- 提取内容：8个责任计划 × 7个交费期间 × 2性别 = 112条记录
- 各交费期间最大承保年龄不同：1年→69岁，3年→65岁，5年/10年→62岁，15年→60岁，20年→59岁，30年→50岁
- 已修正为动态识别每列实际最大有效年龄（非固定69岁）
- 验证：计划1交5年男 → 0岁=58.7，62岁=166.1（题目中"70岁=166.1"实为62岁）
- 输出文件：`f:\workbuddy\test_Jhs\费率边界值汇总.xlsx`（含3个Sheet：明细汇总 + 透视表 + 承保年龄说明）

## API批量测试任务

- API系统：`https://kfzxtb.lmbaoxian.com:13080`
- 接口流程：login → age_rate(saveCustomer) → plan_rate(saveProductExt)
- 关键参数修正：
  - `ensurePlan` 固定传 `"1"`（标准体），费率表"计划0~7"通过 `dutyOptionList` 区分
  - `payModeCode`：交费期间=1年时传`"1"`（一次交清），其他传`"5"`（年交）
  - `insurantSex`：1=男, 2=女
  - `ensurePeriodCode`：终身=TO105
  - `proposalId` 必须从 `age_rate` 响应的 `proposalPlanVOList[0].proposalId` 动态获取
- 224条用例全部PASS，通过率100%，费率偏差均为0%
- 输出文件：`f:\workbuddy\test_Jhs\API测试报告_v2.xlsx`（含2个Sheet：测试结果明细 + 汇总统计）
- 已将 `batch_test.py` 的数据源从原始费率表改为 `费率边界值汇总.xlsx` 的「边界费率汇总」Sheet（更简洁直观）

## 长期记忆更新

- 完整的 API 传值格式、责任计划映射、踩坑经验等已写入 `MEMORY.md`，下次可直接参考使用
